---
import ProjectCard from "./ProjectCard";
const allProjects = await Astro.glob("../contents/projects/*.md");
---

<section class="flex flex-col items-center pt-6 mx-auto">
  <div class="mb-8">
    <h2 class="text-4xl font-bold">Recent Projects</h2>
  </div>
  <ul class="grid grid-cols-2 space-y-12 md:space-y-0 md:space-x-4 lg:px-12">
    {
      allProjects.map((project) => (
        <li>
          <ProjectCard projectData={project.frontmatter} />
        </li>
      ))
    }
  </ul>
</section>
